(* Program that shows Dinoinfos. 18/6/1995 *);
(* by MKC *);


PROGRAM DinoLexikon__Copyright_1993__Manuel_Kiessling_Computing;

USES DOS,CRT;

VAR n,nb,f,z,i,o,ein,stop,bild,command : string;
    ein2,g,l,w : integer;

PROCEDURE intro;
begin;
  command := 'show.exe ';
  bild := 'title.pcx';
  command := '/C'+command+bild;
   SwapVectors;
  exec(GetEnv('COMSPEC'),Command);
   SwapVectors;

  command := 'show.exe ';
  bild := 'title2.pcx';
  command := '/C'+command+bild;
   SwapVectors;
  exec(GetEnv('COMSPEC'),Command);
   SwapVectors;

 ClrScr;
 WriteLn(' C       R       E       D       I       T       S');
 WriteLn('---------------------------------------------------');
 WriteLn;
 WriteLn('Programm........Captain Olliwood');
 WriteLn('DinoBeratung....Simon K.');
 WriteLn('Grafiken........Michael H.');
 WriteLn('                Manuel K.');
 WriteLn('DinoQuellen.....De Agostini`s DINOSAURIER!');
 WriteLn('                Tessloff`s WAS-IST-WAS Dinosaurier');
 WriteLn('Danksagung an...Christian M., CMC');
 ReadLn(stop);
end;

PROCEDURE d1;
 begin;
  n := 'Tyrannosaurus Rex';
 nb := 'Kînig der Tyrannen-Echsen';
  g := 6;
  l := 14;
  f := 'Fleisch';
  z := 'Jura';
  o := 'Nordamerika';
  i := 'Der T-Rex war das schrecklichste Raubtier, das je gelebt hat';
  bild := 'trex.pcx'
 end;

PROCEDURE d2;
 begin;
  n := 'Avaceratops';
 nb := 'gehîrntes Gesicht';
  g := 1;
  l := 3;
  f := 'Pflanzen';
  z := 'Kreide';
  o := 'Nordamerika';
  i := 'Mit seinem Papageienschnabel ri· er niedrige Pflanzen ab';
 end;

PROCEDURE d3;
 begin;
  n := 'Dicraeosaurus';
 nb := 'gegabelte Echse';
  g := 6;
  l := 20;
  f := 'Pflanzen';
  z := 'Jura';
  o := 'Ostafrika';
  i := 'VerhÑltnismÑ·ig kleiner Kopf, kleine Augen und Nasenlîcher';
 end;

PROCEDURE d4;
 begin;
  n := 'Stegosaurus';
 nb := 'Dachechse';
  g := 4;
  l := 7;
  f := 'Pflanzen';
  z := 'Jura';
  o := 'Nordamerika';
  i := 'Den RÅcken entlang verliefen zwei Reihen rautenfîrmiger Knochenplatten';
 end;

PROCEDURE d5;
 begin;
  n := 'Stygimoloch';
 nb := 'gehîrnter Teufel';
  g := 1;
  l := 3;
  f := 'Pflanzen';
  z := 'Kreide';
  o := 'Nordamerika';
  i := 'Die StygimolochmÑnnchen fÅhrten SchÑdelkÑmpfe aus';
 end;

PROCEDURE d6;
 begin;
  n := 'Velociraptor';
 nb := 'schneller PlÅnderer';
  g := 1;
  l := 1;
  f := 'Fleisch';
  z := 'Kreide';
  o := 'Asien';
  i := 'An den Zehen sa·en lange scharfe Krallen, mit denen er seine Beute ri·';
 end;

PROCEDURE d7;
 begin;
  n := 'Brachiosaurus';
 nb := 'Arm-Echse';
  g := 12;
  l := 23;
  f := 'Pflanzen';
  z := 'Jura';
  o := 'Afrika';
  i := 'Um sein Blut den langen Hals hinaufzupumpen, hatte er ein riesiges Herz';
 end;

PROCEDURE d8;
 begin;
  n := 'Lesothosaurus';
 nb := 'Reptil von Lesotho (Entdeckungsort)';
  g := 30;
  l := 1;
  f := 'Pflanzen';
  z := 'Jura';
  o := 'SÅdafrika';
  i := 'Einer der kleinsten Dinosaurier, dafÅr war er blitzschnell';
 end;

PROCEDURE d9;
 begin;
  n := 'Albertosaurus';
 nb := 'Reptil von Alberta (Entdeckungsort)';
  g := 3;
  l := 9;
  f := 'Fleisch';
  z := 'Kreide';
  o := 'Nordamerika';
  i := 'Ein Verwandter von Tyrannosaurus Rex';
 end;

PROCEDURE d10;
 begin;
  n := 'Protoceratops';
 nb := 'erstes Horngesicht';
  g := 1;
  l := 2;
  f := 'Pflanzen';
  z := 'Kreide';
  o := 'Asien';
  i := 'Der Fund eines Protoceratopsnestes bewies, da· Dinosaurier Eier legten';
 end;

PROCEDURE d11;
 begin;
  n := 'Saurolophus';
 nb := 'Echse mit einem Kamm';
  g := 3;
  l := 12;
  f := 'Pflanzen';
  z := 'Kreide';
  o := 'Nordamerika';
  i := 'Hatte einen Hautlappen auf dem Kopf, den er wie ein Frosch aufblasen konnte';
 end;

PROCEDURE d12;
 begin;
  n := 'Syntarsus';
 nb := 'zusammengefÅgter Tarsus (Fu·knochen)';
  g := 1;
  l := 3;
  f := 'Fleisch';
  z := 'Trias';
  o := 'Afrika';
  i := 'Manche Wissenschaftler nehmen an, da· er gefiedert war';
 end;

PROCEDURE d13;
 begin;
  n := 'Iguanodon';
 nb := 'Leguanzahn';
  g := 5;
  l := 10;
  f := 'Pflanzen';
  z := 'Kreide';
  o := 'Europa';
  i := 'Zur Verteidigung hatte er je einen Daumenstachel an jeder Hand';
 end;

PROCEDURE d14;
 begin;
  n := 'Barosaurus';
 nb := 'schwere Echse';
  g := 6;
  l := 27;
  f := 'Pflanzen';
  z := 'Jura';
  o := 'Nordamerika';
  i := 'Man vermutet, B. hatte wegen seines langen Halses mehrere Herzen';
 end;

PROCEDURE d15;
 begin;
  n := 'Gallimimus';
 nb := 'Huhn-Nachahmer';
  g := 3;
  l := 4;
  f := 'Planzen';
  z := 'Kreide';
  o := 'Asien';
  i := 'Er Ñhnelte Ñu·erlich dem Vogel Strau·';
 end;

PROCEDURE d16;
 begin;
  n := 'Allosaurus';
 nb := 'andere Echse';
  g := 5;
  l := 12;
  f := 'Fleisch';
  z := 'Jura';
  o := 'Nordamerika';
  i := 'Er war ein aasfressender Dinosauriern und hatte Riesenhunger';
 end;

PROCEDURE d17;
 begin;
  n := 'Mamenchisaurus';
 nb := 'Mamenchi-Echse (Entdeckungsort)';
  g := 12;
  l := 22;
  f := 'Pflanzen';
  z := 'Jura';
  o := 'Asien';
  i := 'Er hatte einen riesigen Hals, so lang wie drei hintereinanderstehende Autos';
 end;

PROCEDURE d18;
 begin;
  n := 'Coelurus';
 nb := 'Hohlschwanz';
  g := 2;
  l := 2;
  f := 'Fleisch';
  z := 'Jura';
  o := 'Nordamerika';
  i := 'Er besa· hohle Schwanzknochen';
 end;

PROCEDURE d19;
 begin;
  n := 'Deinonychus';
 nb := 'schreckliche Kralle';
  g := 2;
  l := 3;
  f := 'Fleisch';
  z := 'Kreide';
  o := 'Nordamerika';
  i := 'Er erreichte bis zu 40 Stundenkilometer, er war zum Laufen geboren';
 end;

PROCEDURE d20;
 begin;
  n := 'Hylaeosaurus';
 nb := 'Waldechse';
  g := 2;
  l := 4;
  f := 'Pflanzen';
  z := 'Kreide';
  o := 'Europa';
  i := 'Er hatte einen mÑchtigen Panzer, der ihn schÅtzte';
 end;

PROCEDURE d21;
 begin;
  n := 'Tenontosaurus';
 nb := 'Sehnenechse';
  g := 2;
  l := 7;
  f := 'Pflanzen';
  z := 'Kreide';
  o := 'Nordamerika';
  i := 'Er war ein Papageienmaul-Dinosaurier';
 end;

PROCEDURE zeige;
 begin;
  WriteLn('Name des Dinosauriers : ',n);
  WriteLn('Bedeutung des Namens  : ',nb);
  WriteLn('Grî·e                 : ',g,'m');
  WriteLn('LÑnge                 : ',l,'m');
  WriteLn('Fra·                  : ',f);
  WriteLn('Zeitperiode           : ',z);
  WriteLn('Lebensraum            : ',o);
  WriteLn('Besonderheiten        : ',i);
   ReadLn(stop);
  command := 'show.exe ';
  command := '/C'+command+bild;
   SwapVectors;
  exec(GetEnv('COMSPEC'),Command);
   SwapVectors;
 end;

PROCEDURE name;
 begin;
  ClrScr;
   WriteLn('DinoSuche nach Namen');
   WriteLn('--------------------');
   WriteLn('');
   Write('Geben Sie den Namen des gewÅnschten Dinosauriers ein : ');
   ReadLn(ein);
   WriteLn;
    d1;
    IF ein = n THEN zeige;
    d2;
    IF ein = n THEN zeige;
    d3;
    IF ein = n THEN zeige;
    d4;
    IF ein = n THEN zeige;
    d5;
    IF ein = n THEN zeige;
    d6;
    IF ein = n THEN zeige;
    d7;
    IF ein = n THEN zeige;
    d8;
    IF ein = n THEN zeige;
    d9;
    IF ein = n THEN zeige;
    d10;
    IF ein = n THEN zeige;
    d11;
    IF ein = n THEN zeige;
    d12;
    IF ein = n THEN zeige;
    d13;
    IF ein = n THEN zeige;
    d14;
    IF ein = n THEN zeige;
    d15;
    IF ein = n THEN zeige;
    d16;
    IF ein = n THEN zeige;
    d17;
    IF ein = n THEN zeige;
    d18;
    IF ein = n THEN zeige;
    d19;
    IF ein = n THEN zeige;
    d20;
    IF ein = n THEN zeige;
    d21;
    IF ein = n THEN zeige;
end;

PROCEDURE MinG;
 begin;
  ClrScr;
   WriteLn('DinoSuche nach minimaler Grî·e');
   WriteLn('------------------------------');
   WriteLn('');
   Write('Geben Sie Grî·e ein, die die Dinosaurier mindestens haben mÅssen : ');
   ReadLn(ein2);
   WriteLn;
    d1;
    IF ein2<= g THEN WriteLn(n,' ',g,'m');
    d2;
    IF ein2<= g THEN WriteLn(n,' ',g,'m');
    d3;
    IF ein2<= g THEN WriteLn(n,' ',g,'m');
    d4;
    IF ein2<= g THEN WriteLn(n,' ',g,'m');
    d5;
    IF ein2<= g THEN WriteLn(n,' ',g,'m');
    d6;
    IF ein2<= g THEN WriteLn(n,' ',g,'m');
    d7;
    IF ein2<= g THEN WriteLn(n,' ',g,'m');
    d8;
    IF ein2<= g THEN WriteLn(n,' ',g,'m');
    d9;
    IF ein2<= g THEN WriteLn(n,' ',g,'m');
    d10;
    IF ein2<= g THEN WriteLn(n,' ',g,'m');
    d11;
    IF ein2<= g THEN WriteLn(n,' ',g,'m');
    d12;
    IF ein2<= g THEN WriteLn(n,' ',g,'m');
    d13;
    IF ein2<= g THEN WriteLn(n,' ',g,'m');
    d14;
    IF ein2<= g THEN WriteLn(n,' ',g,'m');
    d15;
    IF ein2<= g THEN WriteLn(n,' ',g,'m');
    d16;
    IF ein2<= g THEN WriteLn(n,' ',g,'m');
    d17;
    IF ein2<= g THEN WriteLn(n,' ',g,'m');
    d18;
    IF ein2<= g THEN WriteLn(n,' ',g,'m');
    d19;
    IF ein2<= g THEN WriteLn(n,' ',g,'m');
    d20;
    IF ein2<= g THEN WriteLn(n,' ',g,'m');
    d21;
    IF ein2<= g THEN WriteLn(n,' ',g,'m');
     ReadLn(stop);
end;

PROCEDURE MaxG;
 begin;
  ClrScr;
   WriteLn('DinoSuche nach maximaler Grî·e');
   WriteLn('------------------------------');
   WriteLn('');
   Write('Geben Sie Grî·e ein, die die Dinosaurier hîchstens haben dÅrfen : ');
   ReadLn(ein2);
   WriteLn;
    d1;
    IF ein2>= g THEN WriteLn(n,' ',g,'m');
    d2;
    IF ein2>= g THEN WriteLn(n,' ',g,'m');
    d3;
    IF ein2>= g THEN WriteLn(n,' ',g,'m');
    d4;
    IF ein2>= g THEN WriteLn(n,' ',g,'m');
    d5;
    IF ein2>= g THEN WriteLn(n,' ',g,'m');
    d6;
    IF ein2>= g THEN WriteLn(n,' ',g,'m');
    d7;
    IF ein2>= g THEN WriteLn(n,' ',g,'m');
    d8;
    IF ein2>= g THEN WriteLn(n,' ',g,'m');
    d9;
    IF ein2>= g THEN WriteLn(n,' ',g,'m');
    d10;
    IF ein2>= g THEN WriteLn(n,' ',g,'m');
    d11;
    IF ein2>= g THEN WriteLn(n,' ',g,'m');
    d12;
    IF ein2>= g THEN WriteLn(n,' ',g,'m');
    d13;
    IF ein2>= g THEN WriteLn(n,' ',g,'m');
    d14;
    IF ein2>= g THEN WriteLn(n,' ',g,'m');
    d15;
    IF ein2>= g THEN WriteLn(n,' ',g,'m');
    d16;
    IF ein2>= g THEN WriteLn(n,' ',g,'m');
    d17;
    IF ein2>= g THEN WriteLn(n,' ',g,'m');
    d18;
    IF ein2>= g THEN WriteLn(n,' ',g,'m');
    d19;
    IF ein2>= g THEN WriteLn(n,' ',g,'m');
    d20;
    IF ein2>= g THEN WriteLn(n,' ',g,'m');
    d21;
    IF ein2>= g THEN WriteLn(n,' ',g,'m');
     ReadLn(stop);
end;

PROCEDURE MinL;
 begin;
  ClrScr;
   WriteLn('DinoSuche nach minimaler LÑnge');
   WriteLn('------------------------------');
   WriteLn('');
   Write('Geben Sie LÑnge ein, die die Dinosaurier mindestens haben mÅssen : ');
   ReadLn(ein2);
   WriteLn;
    d1;
    IF ein2<= l THEN WriteLn(n,' ',l,'m');
    d2;
    IF ein2<= l THEN WriteLn(n,' ',l,'m');
    d3;
    IF ein2<= l THEN WriteLn(n,' ',l,'m');
    d4;
    IF ein2<= l THEN WriteLn(n,' ',l,'m');
    d5;
    IF ein2<= l THEN WriteLn(n,' ',l,'m');
    d6;
    IF ein2<= l THEN WriteLn(n,' ',l,'m');
    d7;
    IF ein2<= l THEN WriteLn(n,' ',l,'m');
    d8;
    IF ein2<= l THEN WriteLn(n,' ',l,'m');
    d9;
    IF ein2<= l THEN WriteLn(n,' ',l,'m');
    d10;
    IF ein2<= l THEN WriteLn(n,' ',l,'m');
    d11;
    IF ein2<= l THEN WriteLn(n,' ',l,'m');
    d12;
    IF ein2<= l THEN WriteLn(n,' ',l,'m');
    d13;
    IF ein2<= l THEN WriteLn(n,' ',l,'m');
    d14;
    IF ein2<= l THEN WriteLn(n,' ',l,'m');
    d15;
    IF ein2<= l THEN WriteLn(n,' ',l,'m');
    d16;
    IF ein2<= l THEN WriteLn(n,' ',l,'m');
    d17;
    IF ein2<= l THEN WriteLn(n,' ',l,'m');
    d18;
    IF ein2<= l THEN WriteLn(n,' ',l,'m');
    d19;
    IF ein2<= l THEN WriteLn(n,' ',l,'m');
    d20;
    IF ein2<= l THEN WriteLn(n,' ',l,'m');
    d21;
    IF ein2<= l THEN WriteLn(n,' ',l,'m');
     ReadLn(stop);
end;

PROCEDURE MaxL;
begin;
 ClrScr;
  WriteLn('DinoSuche nach maximaler LÑnge');
  WriteLn('------------------------------');
  WriteLn('');
  Write('Geben Sie LÑnge ein, die die Dinosaurier hîchstens haben dÅrfen : ');
  ReadLn(ein2);
  WriteLn;
   d1;
   IF ein2>= l THEN WriteLn(n,' ',l,'m');
   d2;
   IF ein2>= l THEN WriteLn(n,' ',l,'m');
   d3;
   IF ein2>= l THEN WriteLn(n,' ',l,'m');
   d4;
   IF ein2>= l THEN WriteLn(n,' ',l,'m');
   d5;
   IF ein2>= l THEN WriteLn(n,' ',l,'m');
   d6;
   IF ein2>= l THEN WriteLn(n,' ',l,'m');
   d7;
   IF ein2>= l THEN WriteLn(n,' ',l,'m');
   d8;
   IF ein2>= l THEN WriteLn(n,' ',l,'m');
   d9;
   IF ein2>= l THEN WriteLn(n,' ',l,'m');
   d10;
   IF ein2>= l THEN WriteLn(n,' ',l,'m');
   d11;
   IF ein2>= l THEN WriteLn(n,' ',l,'m');
   d12;
   IF ein2>= l THEN WriteLn(n,' ',l,'m');
   d13;
   IF ein2>= l THEN WriteLn(n,' ',l,'m');
   d14;
   IF ein2>= l THEN WriteLn(n,' ',l,'m');
   d15;
   IF ein2>= l THEN WriteLn(n,' ',l,'m');
   d16;
   IF ein2>= l THEN WriteLn(n,' ',l,'m');
   d17;
   IF ein2>= l THEN WriteLn(n,' ',l,'m');
   d18;
   IF ein2>= l THEN WriteLn(n,' ',l,'m');
   d19;
   IF ein2>= l THEN WriteLn(n,' ',l,'m');
   d20;
   IF ein2>= l THEN WriteLn(n,' ',l,'m');
   d21;
   IF ein2>= l THEN WriteLn(n,' ',l,'m');
    ReadLn(stop);
end;

PROCEDURE nahr;
begin;
 ClrScr;
   WriteLn('DinoSuche nach Nahrungsgewohnheit');
   WriteLn('---------------------------------');
   WriteLn('');
   Write('Geben Sie ein, was die Dinos fressen dÅrfen(Fleisch/Pflanzen) : ');
   ReadLn(ein);
    WriteLn;
    d1;
    IF ein = f THEN WriteLn(n,': ',f,'fresser');
    d2;
    IF ein = f THEN WriteLn(n,': ',f,'fresser');
    d3;
    IF ein = f THEN WriteLn(n,': ',f,'fresser');
    d4;
    IF ein = f THEN WriteLn(n,': ',f,'fresser');
    d5;
    IF ein = f THEN WriteLn(n,': ',f,'fresser');
    d6;
    IF ein = f THEN WriteLn(n,': ',f,'fresser');
    d7;
    IF ein = f THEN WriteLn(n,': ',f,'fresser');
    d8;
    IF ein = f THEN WriteLn(n,': ',f,'fresser');
    d9;
    IF ein = f THEN WriteLn(n,': ',f,'fresser');
    d10;
    IF ein = f THEN WriteLn(n,': ',f,'fresser');
    d11;
    IF ein = f THEN WriteLn(n,': ',f,'fresser');
    d12;
    IF ein = f THEN WriteLn(n,': ',f,'fresser');
    d13;
    IF ein = f THEN WriteLn(n,': ',f,'fresser');
    d14;
    IF ein = f THEN WriteLn(n,': ',f,'fresser');
    d15;
    IF ein = f THEN WriteLn(n,': ',f,'fresser');
    d16;
    IF ein = f THEN WriteLn(n,': ',f,'fresser');
    d17;
    IF ein = f THEN WriteLn(n,': ',f,'fresser');
    d18;
    IF ein = f THEN WriteLn(n,': ',f,'fresser');
    d19;
    IF ein = f THEN WriteLn(n,': ',f,'fresser');
    d20;
    IF ein = f THEN WriteLn(n,': ',f,'fresser');
    d21;
    IF ein = f THEN WriteLn(n,': ',f,'fresser');
     ReadLn(stop);
end;

PROCEDURE zeit;
begin;
 ClrScr;
   WriteLn('DinoSuche nach Zeitabschnitten');
   WriteLn('------------------------------');
   WriteLn('');
   Write('Geben Sie ein, in welcher Zeit die Dinos leben (Trias/Kreide/Jura) : ');
   ReadLn(ein);
    WriteLn;
    d1;
    IF ein = z THEN WriteLn(n,': ',z,'zeit');
    d2;
    IF ein = z THEN WriteLn(n,': ',z,'zeit');
    d3;
    IF ein = z THEN WriteLn(n,': ',z,'zeit');
    d4;
    IF ein = z THEN WriteLn(n,': ',z,'zeit');
    d5;
    IF ein = z THEN WriteLn(n,': ',z,'zeit');
    d6;
    IF ein = z THEN WriteLn(n,': ',z,'zeit');
    d7;
    IF ein = z THEN WriteLn(n,': ',z,'zeit');
    d8;
    IF ein = z THEN WriteLn(n,': ',z,'zeit');
    d9;
    IF ein = z THEN WriteLn(n,': ',z,'zeit');
    d10;
    IF ein = z THEN WriteLn(n,': ',z,'zeit');
    d11;
    IF ein = z THEN WriteLn(n,': ',z,'zeit');
    d12;
    IF ein = z THEN WriteLn(n,': ',z,'zeit');
    d13;
    IF ein = z THEN WriteLn(n,': ',z,'zeit');
    d14;
    IF ein = z THEN WriteLn(n,': ',z,'zeit');
    d15;
    IF ein = z THEN WriteLn(n,': ',z,'zeit');
    d16;
    IF ein = z THEN WriteLn(n,': ',z,'zeit');
    d17;
    IF ein = z THEN WriteLn(n,': ',z,'zeit');
    d18;
    IF ein = z THEN WriteLn(n,': ',z,'zeit');
    d19;
    IF ein = z THEN WriteLn(n,': ',z,'zeit');
    d20;
    IF ein = z THEN WriteLn(n,': ',z,'zeit');
    d21;
    IF ein = z THEN WriteLn(n,': ',z,'zeit');
     ReadLn(stop);
end;

PROCEDURE raum;
begin;
 ClrScr;
   WriteLn('DinoSuche nach LebensrÑumen');
   WriteLn('---------------------------');
   WriteLn('');
   Write('Geben Sie ein, wo die Dinosaurier leben sollen(Kontinent) : ');
   ReadLn(ein);
    WriteLn;
    d1;
    IF ein = o THEN WriteLn(n,': ',o,'');
    d2;
    IF ein = o THEN WriteLn(n,': ',o,'');
    d3;
    IF ein = o THEN WriteLn(n,': ',o,'');
    d4;
    IF ein = o THEN WriteLn(n,': ',o,'');
    d5;
    IF ein = o THEN WriteLn(n,': ',o,'');
    d6;
    IF ein = o THEN WriteLn(n,': ',o,'');
    d7;
    IF ein = o THEN WriteLn(n,': ',o,'');
    d8;
    IF ein = o THEN WriteLn(n,': ',o,'');
    d9;
    IF ein = o THEN WriteLn(n,': ',o,'');
    d10;
    IF ein = o THEN WriteLn(n,': ',o,'');
    d11;
    IF ein = o THEN WriteLn(n,': ',o,'');
    d12;
    IF ein = o THEN WriteLn(n,': ',o,'');
    d13;
    IF ein = o THEN WriteLn(n,': ',o,'');
    d14;
    IF ein = o THEN WriteLn(n,': ',o,'');
    d15;
    IF ein = o THEN WriteLn(n,': ',o,'');
    d16;
    IF ein = o THEN WriteLn(n,': ',o,'');
    d17;
    IF ein = o THEN WriteLn(n,': ',o,'');
    d18;
    IF ein = o THEN WriteLn(n,': ',o,'');
    d19;
    IF ein = o THEN WriteLn(n,': ',o,'');
    d20;
    IF ein = o THEN WriteLn(n,': ',o,'');
    d21;
    IF ein = o THEN WriteLn(n,': ',o,'');
     ReadLn(stop);
end;

PROCEDURE hauptmenu;
begin;
 w := 1;
 ClrScr;
  WriteLn('DinoLexikon V1.0 HAUPTMENU');
  WriteLn('--------------------------');
  WriteLn('');
  WriteLn('DinoInfo anzeigen...1');
  WriteLn('Minimale Grî·e......2');
  WriteLn('Maximale Grî·e......3');
  WriteLn('Minimale LÑnge......4');
  WriteLn('Maximale LÑnge......5');
  WriteLn('Nahrung.............6');
  WriteLn('Zeitperiode.........7');
  WriteLn('Lebensraum..........8');
  WriteLn('Programm verlassen..Q');
  WriteLn('');
   Write('Geben Sie die Nummer der gewÅnschten Funktion an : ');
   ReadLn(ein);
    IF ein = '1' THEN name;
    IF ein = '2' THEN MinG;
    IF ein = '3' THEN MaxG;
    IF ein = '4' THEN MinL;
    IF ein = '5' THEN MaxL;
    IF ein = '6' THEN nahr;
    IF ein = '7' THEN zeit;
    IF ein = '8' THEN raum;
    IF ein = 'Q' THEN w := 0;
end;

 LABEL weiter;
BEGIN
{$M 8192,0,0}
 intro;
 w := 1;
  weiter:
   IF w <> 0 THEN hauptmenu;
   IF w <> 0 THEN goto weiter;
 ClrScr;
END.